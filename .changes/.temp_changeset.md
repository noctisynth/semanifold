semifold: minor:feat
---

Adds an AI agent feature to automatically generate changesets based on git diffs. The agent can use OpenAI or Anthropic-compatible APIs, configured via `.changes/config.toml` and `.env`. Includes provider presets (minimax, deepseek, anthropic) and interactive setup via `semifold init --agent`. CI workflows simplified to use the `setup-semifold` action.
