---
semifold-core: "minor:fix"
semifold-engine: "patch:fix"
---

Use `nodejs` as the canonical built-in ecosystem identity so domain serialization, existing package configuration, adapter lookup, and future plugin registration share one stable value without aliases.
