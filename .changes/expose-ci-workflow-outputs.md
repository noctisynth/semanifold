---
semifold: patch:fix
---

Make `smif ci` reuse the version and publish execution paths so GitHub Actions receives the same versioned outputs, and expose them through stable step and job output mappings in generated workflows.
