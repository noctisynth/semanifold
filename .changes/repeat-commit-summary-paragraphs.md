---
semifold: minor:feat
---

Allow `smif commit -m` to be repeated, treating each value as an ordered summary paragraph that the
default changelog template renders as a continuation paragraph within the same changeset list item.
