---
"@semifold/docs": "patch:fix"
---

Resolve installation-script defaults from the latest stable `semifold-vX.Y.Z` binary Release instead of the repository-wide latest Release, and normalize explicit versions with or without a leading `v`.
